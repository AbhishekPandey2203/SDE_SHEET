#include<bits/stdc++.h>
using namespace std;

void sorted( vector<int>&binary, int target)
{
     
}

int main()
{
    int n;
    cout<<"Enter the no of elements"<<endl;
    cin>>n;
   cout<<"Enter the target"<<endl;
   int target;
   cin>>target;
    vector<int>binary(n);

    cout<<"Enter the binary search array input"<<endl;

    for(int i=0;i<n;i++)
    {
        cin>>binary[i];
    }
   //calling the 

   sorted(binary,target);
    










}